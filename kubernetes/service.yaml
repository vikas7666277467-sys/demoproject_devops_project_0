apiVersion: v1
kind: Service
metadata:
  name: flask-app
  namespace: demo
  labels:
    app.kubernetes.io/name: flask-app
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: flask-app
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080

